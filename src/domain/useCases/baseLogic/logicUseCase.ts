const logicUseCase = async (input: string): Promise<string> => {
    // async logic here
    // e.g. call to external service
    //
    // manipulate response
    // return manipulated response
    return input;
}

export {
    logicUseCase,
}
